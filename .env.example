# Supabase(서울 리전) Postgres 연결 — 팀이 실제 값으로 채움
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/postgres?schema=public"

# 인증/문자 (Solapi) — MVP 휴대폰 OTP·알림
SOLAPI_API_KEY=""
SOLAPI_API_SECRET=""

# 데모 모드(인메모리 저장소) — DB 없이 UI 실행
DEMO_MODE="true"
